# Production / test with Vercel backend:
VITE_API_URL=https://vlmbackend.vercel.app/api

# Local backend only (npm run dev in backend/) — comment line above, use:
# VITE_API_URL=http://localhost:5000/api

# Or leave empty — Vite proxy forwards /api → localhost:5000
# VITE_API_URL=
